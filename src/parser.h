#ifndef PARSER_H_
#define PARSER_H_
#include "letter.h"
#include "word.h"
#include "phrase.h"
#include "mat.h"

class parser
{
	public:
		//default constructor
		parser()
		{
		}
		
		//destructor
		~parser()
		{
		}
		
		//copy constructor
		parser(const parser& p)
		{
		}
		
		//copy assignment
		parser& operator=(const parser& p)
		{
			
			return *this;
		}
		
};

#endif